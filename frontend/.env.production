# ===========================================
# CMC Delal - Frontend Production Environment
# ===========================================
# This file is used when running `npm run build`
# Leave empty for same-origin (backend serves frontend)
# Set to a different URL only if frontend and backend are on separate domains
VITE_API_BASE_URL=
