{
  "name": "cmc-delal",
  "version": "1.0.0",
  "private": true,
  "description": "CMC Delal - Secure Ethiopian Broker Platform",
  "scripts": {
    "build": "cd frontend && npm ci && npm run build",
    "start": "node backend/server.js",
    "postinstall": "cd backend && npm ci --production",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && API_BASE_URL=* NODE_ENV=development node server.js",
    "dev:frontend": "cd frontend && npm run dev"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "concurrently": "^9.2.3"
  }
}
